import React from 'react';
import CommonTitle from '../../Components/CommonTitle';

const Login = () => {
    return (
        <CommonTitle>
            Login
        </CommonTitle>
    );
};

export default Login;